package chess.game;

import chess.model.Board;
import chess.model.Move;

/**
 * Placeholder - in GUI, human moves are performed by UI, not here.
 */
public class HumanPlayer extends Player {
    @Override
    public Move chooseMove(Board board) {
        return null;
    }
}
